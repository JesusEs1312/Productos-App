import  {createTheme} from '@material-ui/core/styles';

//Constante que tendra los colores de la interfaz
const theme = createTheme ({
    palette : {
        primary: {
            light : '#4f83cc',
            main  : '#0277bd',
            dark  : '#002f6c'
        }
    }
});

export default theme;